import './Header.css'

function Header() {
    /* 
        Simple header component.
        Ideally, this would include navigation links and user info.
    */
    return (
        <header>
            <h1>UP!</h1>
            <p>A SaaS Platform</p>
        </header>
    );
}

export default Header;